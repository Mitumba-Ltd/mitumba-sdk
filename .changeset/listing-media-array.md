---
"@mitumba/sdk": minor
---

Add optional `media: string[]` to `Listing`, `SearchResult`, and `WishlistListing` — array of all image CDN URLs ordered by position, enabling swipeable carousels in list views.
