---
"@mitumba/sdk": minor
---

Add `size` field to `Listing` (as `string | null`), `CreateListingInput` (as optional `string`), and `CartItem` (as `string | null`). Also adds `store_slug` to `CartItem`.
