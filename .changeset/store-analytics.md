---
"@mitumba/sdk": minor
---

Add `sdk.stores.getAnalytics(storeId, period?)` — get store analytics with revenue, orders breakdown, traffic sources, top listings, geography, and STI trend (GET /listings/stores/:storeId/analytics).

New exported type: `StoreAnalytics`
