---
"@mitumba/sdk": minor
---

Add `sdk.vazi.getOutfit(outfitId)` — get a single outfit with full item details (GET /vazi/outfits/:id).

New exported types: `VAZIOutfitDetail`, `VAZIOutfitDetailItem`
